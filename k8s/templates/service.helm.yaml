---
apiVersion: v1
kind: Service
metadata:
  name: proxy
  namespace: "{{ .Release.Namespace }}"
spec:
  selector:
    app: proxy
  ports:
    - name: proxy
      protocol: TCP
      port: 8081
      targetPort: proxy
    - name: ui
      protocol: TCP
      port: 9047
      targetPort: ui
